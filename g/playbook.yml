---
- hosts: all
  gather_facts: False
  tasks:
      - debug:
           var: inventory_var

      - debug:
           var: inventory_groups_var

      - debug:
           var: inventory_hosts_var

      - debug:
           var: groups_var

      - debug:
           var: hosts_var

      - debug:
           var: plays_var

      - debug:
           var: tasks_var
        vars:
           tasks_var: "this a task variable"

      - debug:
           var: roles_var

      - debug:
           var: facts_var


